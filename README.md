Infinite Scrolling for GitBook
==============

You can use install it via **NPM**:

```
$ npm install gitbook-plugin-infinitescroll
```

And use it for your book with in the book.json:

```
{
    "plugins": ["infinitescroll"]
}
```